# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.venv-mjx
.venv-cuda

# 학습 로그 (대용량, 재생성 가능)
logs/

# 실행 폴더: 체크포인트(.pkl)·TensorBoard 이벤트 (대용량, 재생성 가능)
# 최종 모델(pipeline_*.pkl)은 루트에 별도 커밋
runs/

# 자동 실험 큐 스크립트 (sudo 비번 포함 가능 — 공개 금지). 템플릿: run_queue.sh.example
run_queue.sh

# 레거시/중복 바이너리 (히스토리엔 남아있으나 추적 해제)
ppo_ant.zip
pipeline_pusher.pkl
pipeline_g1_rough_100M.pkl
rollout.mp4
rollout_pusher.mp4
